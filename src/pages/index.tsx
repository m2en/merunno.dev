import { Center } from '@chakra-ui/react';
import { Header } from '../components/Header';

export default function Home() {
  return (
    <main>
      <Center h={'500px'}>
        <Header />
      </Center>
    </main>
  );
}
